
- 赶快把视频的事搞了，从而可以让我快点看到结果视频
	- 目前想的是让我的codex来做这件事（感觉这样更好了，把原本让claude一个人做的事分开了，便于划分指责界限，也方便写prompt）
	- 初步计划是让codex定时看（但不改）其它codex的产物，同时到服务器上找到对应的产物，然后拉下来再制作成一个html便于观看
	- 但问题1: 是不是也得定时唤醒它，然后让它读特定的文件（如果这样唤醒prompt就不能按照目前的几个prompt来了，因为它的职责变了），同时需要写新的prompt和约束规则
	- 问题2：这个codex用不用参照一下之前claude的一部分prompt（因为学长的claude之前也负责这个），同时COMMS文件里有一个## 📌 待用户视觉判断板块

> **规则（全员遵守，用户 06-13 指令）**：任何需要用户肉眼裁决的点，**一律登记到本看板**， 不要只埋在 codex 回报正文里。每条三要素：①本地路径（`多视角/视频/<run>/`，优先 contact 图） ②**一句话问题**（最好能 y/n 或 A/B 回答）③谁在等、它解锁哪条线。 用户裁决后，审查方把该条标 ✅ 并把结论落进 DIRECTIVE。视觉=用户最终裁决权（CLAUDE.md 铁律），此看板是其唯一入口。

**当前待判（最新在上）：**

- 🟡 **W2 放置 gate 五联**（待 Training 重启采样产出）：`视频/w2_frozenvae_step1500_single_5col_gate_v0/` 问：`ours_true` 里非 ego 玩家是否比 `ours_blank`/`ours_shuffled` 更出现在与 GT 一致的位置？(y/n) → 解锁：**人物控制是否生效**的最终判断（虚化已解决，控制是 W2 最后一块拼图）。
- ✅ **W2 虚化对比**（已裁决 06-13 18:15）：人物虚化【基本解决】；遗留发糊/噪点列 post-gate 优化。如需复看新窗在此追加。
可以明确它的工作就是巡查一遍，在这加一下最新要看的重点，然后同时把结果拉回对应路径


- 再把我的claude约束优化了，转变它的职责为审查并补充主审查claude（注意不是QXQ-Claude的疏漏）（因此我的claude也应该几乎执行与主审查相同的动作（比如同样））