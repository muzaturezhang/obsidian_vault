这个文档很关键，它把你现在的判断直接简化了：

你现在不是在“盲找数据”，而是在验证这件事：

> **服务器上已经有完整 CS:GO 原始数据；训练集只有几百个 clip，大概率是因为训练 manifest / split 只选了其中一小部分。你的任务就是把“原始数据规模”和“训练实际使用规模”对上。**

根据文档，三个路径含义是：

```text
/mnt/data/csgo-datasets
主数据源。顶层是 match/map hash，下面是具体 game/episode 数据。

/mnt/data/csgo-datasets-fullsubset
更适合先做 v0 smoke test。里面结构更清楚：
某个 hash 目录下有 navmesh.json、static_props.json；
episode 里有每个玩家的 .mp4、_depth.mkv、_seg.mkv、_motion.bin、玩家状态 .json、_player_visibility.json。

/mnt/data/csgo-datasets-oss
OSS 挂载镜像，结构类似 csgo-datasets；训练优先用 CPFS 路径。
```

所以你现在的优先级应该改成：

```text
第一优先级：/mnt/data/csgo-datasets-fullsubset
第二优先级：/mnt/data/csgo-datasets
暂时不要碰：/mnt/data/csgo-datasets-oss，除非前两个路径不够
```

---

# 你现在具体该怎么做

## 第一步：先看 fullsubset 的真实结构

你先跑这些只读命令：

```bash
find /mnt/data/csgo-datasets-fullsubset -maxdepth 3 -type d | head -n 100
```

然后看文档里提到的 hash 目录是否存在：

```bash
ls -lah /mnt/data/csgo-datasets-fullsubset
find /mnt/data/csgo-datasets-fullsubset -maxdepth 2 -type f | head -n 50
```

文档里给的示例 hash 是：

```text
32f1644d4f42c29d/030a506e5e77459c8d5078a06eec61ee
```

所以你可以检查：

```bash
ls -lah /mnt/data/csgo-datasets-fullsubset/32f1644d4f42c29d/030a506e5e77459c8d5078a06eec61ee
find /mnt/data/csgo-datasets-fullsubset/32f1644d4f42c29d/030a506e5e77459c8d5078a06eec61ee -maxdepth 3 -type f | head -n 100
```

如果路径不存在，就说明文档是示例，不代表你当前机器一定有这个具体 hash。那就继续用 `find` 找。

---

## 第二步：确认每个 clip 的文件组

你重点查这几类文件：

```bash
find /mnt/data/csgo-datasets-fullsubset -type f | grep -E "\.mp4$|_depth\.mkv$|_seg\.mkv$|_motion\.bin$|_player_visibility\.json$|\.json$" | head -n 100
```

再统计文件类型：

```bash
find /mnt/data/csgo-datasets-fullsubset -type f \
| sed -E 's/.*(_depth\.mkv|_seg\.mkv|_motion\.bin|_player_visibility\.json|\.mp4|\.json)$/\1/' \
| sort | uniq -c | sort -nr
```

你看到的理想情况应该类似：

```text
xxx.mp4
xxx_depth.mkv
xxx_seg.mkv
xxx_motion.bin
xxx.json
xxx_player_visibility.json
```

也就是说，一个 clip 不是一个文件，而是一组同 basename 的多模态文件。

---

## 第三步：你的 metadata 脚本要加上 seg 和 visibility

我之前给你的字段里有 RGB、depth、hud、motion、json、episode_info。  
但根据这个文档，你们这个数据更应该统计：

```text
RGB: .mp4
depth: _depth.mkv
seg: _seg.mkv
motion: _motion.bin
player visibility: _player_visibility.json
player state: .json
map prior: navmesh.json / static_props.json
```

所以字段应该改成：

```text
clip_id
root
match_hash
map_hash
episode_dir
basename
episode
team
player
inst

has_rgb
has_depth
has_seg
has_motion
has_player_state
has_player_visibility

rgb_path
depth_path
seg_path
motion_path
player_state_path
player_visibility_path

has_navmesh
has_static_props
navmesh_path
static_props_path

rgb_size_mb
depth_size_mb
seg_size_mb
motion_size_mb
duration_sec
width
height
fps
num_frames
```

这里注意：  
`navmesh.json` 和 `static_props.json` 不是每个 player clip 一个，而更可能是 **map/game 级别共享文件**。所以不要把它们当成每个 clip 的独立文件，而是从当前 clip 的上级目录往上找。

---

## 第四步：先只统计 fullsubset

你现在先不要全量扫两个大目录。为了 3 小时任务，先做这个：

```bash
python tools/scan_csgo_metadata.py \
  --roots /mnt/data/csgo-datasets-fullsubset \
  --out_csv outputs/fullsubset_metadata.csv \
  --out_json outputs/fullsubset_summary.json
```

如果 fullsubset 跑通，再加主数据源：

```bash
python tools/scan_csgo_metadata.py \
  --roots /mnt/data/csgo-datasets-fullsubset /mnt/data/csgo-datasets \
  --out_csv outputs/all_metadata.csv \
  --out_json outputs/all_summary.json
```

---

# 你要验证的核心结论

这份文档已经告诉你：

```text
原始数据里已经包含 RGB、depth、seg、motion、player visibility、地图先验。
```

所以你现在不是要证明“有没有数据”，而是要证明：

```text
1. fullsubset 里到底有多少完整 clip group；
2. csgo-datasets 里到底有多少完整 clip group；
3. 当前训练 manifest 里实际用了多少 clip；
4. 训练 manifest 用的 clip 是不是只来自某个 hash / 某些 episode / 某些玩家；
5. 大量原始数据为什么没有进入训练。
```

最可能的解释是：

```text
文件数量很多 ≠ 训练 clip 数很多。

因为一个 clip 会对应多个文件：
.mp4
_depth.mkv
_seg.mkv
_motion.bin
.json
_player_visibility.json

所以 6000 个文件可能只有 1000 个 clip group。

另外，训练 manifest 可能只选了其中几百个 clip 做 sanity / v0 / smoke test。
```

---

# 现在你的 3 小时计划应该改成这样

## 0:00–0:30：确认 fullsubset 结构

跑：

```bash
find /mnt/data/csgo-datasets-fullsubset -maxdepth 3 -type d | head -n 100
find /mnt/data/csgo-datasets-fullsubset -type f | head -n 100
find /mnt/data/csgo-datasets-fullsubset -type f | grep -E "\.mp4$|_depth\.mkv$|_seg\.mkv$|_motion\.bin$|_player_visibility\.json$" | head -n 100
```

产出：

```text
确认 fullsubset 是否真的有 RGB/depth/seg/motion/visibility。
```

---

## 0:30–1:00：找训练 manifest

在项目目录跑：

```bash
find . -maxdepth 5 -type f | grep -Ei "manifest|train|val|split|list|dataset|jsonl|csv|txt"
```

重点找：

```text
train list
val list
manifest
dataset split
```

产出：

```text
当前训练到底用了多少条。
```

---

## 1:00–2:00：写 metadata 统计脚本

脚本优先支持：

```text
.mp4
_depth.mkv
_seg.mkv
_motion.bin
_player_visibility.json
普通 .json
navmesh.json
static_props.json
```

第一版不要 ffprobe，先只统计文件存在性和大小。

---

## 2:00–2:30：生成 fullsubset summary

输出：

```text
outputs/fullsubset_metadata.csv
outputs/fullsubset_summary.json
outputs/candidate_complete_clips.csv
```

筛选标准：

```text
has_rgb == True
has_depth == True
has_seg == True
has_motion == True
has_player_state == True
has_player_visibility == True
```

---

## 2:30–3:00：写结论

你最后可以这样汇报给学长：

```text
我看了文档和服务器路径，目前数据不需要从零采集。重点数据在 /mnt/data/csgo-datasets-fullsubset 和 /mnt/data/csgo-datasets，其中 fullsubset 更适合先做 v0 smoke test。里面每个玩家 clip 不是单文件，而是一组 RGB mp4、depth、seg、motion、player state json 和 visibility json；navmesh/static_props 是地图级先验。

我现在正在做两件事：
1. 找当前训练 manifest，确认训练实际用了多少 clip；
2. 写 metadata 脚本，把原始目录按 basename group 统计成 csv/json，区分完整 clip 和缺字段 clip。

初步判断，“原始数据很多但训练集只有几百个 clip”可能是因为：
- 原始目录文件数多，但一个 clip 对应多个 modality 文件；
- 训练 manifest 只选了某个 subset；
- 当前 v0/smoke test 可能只用了少量 episode/player；
- 只有完整包含 RGB/depth/seg/motion/visibility 的样本才适合进入候选训练集。

我会输出：
- fullsubset_metadata.csv
- fullsubset_summary.json
- candidate_complete_clips.csv
并记录哪些字段还需要确认，比如 clip 定义、duration 来源、训练 split 规则。
```

---

# 给 Codex 的更新版 prompt

你现在可以直接把这个丢给 Codex：

```text
你现在是一个谨慎的科研工程助手。任务是搞清楚“原始 CS:GO 数据很多，但当前训练集只有几百个 clip”到底是怎么回事，并写 metadata 统计脚本。

背景文档说明：
服务器已经挂载可用 CS:GO 数据集，不需要从零采集。数据中包含 RGB、depth、seg、motion、player visibility 和地图先验。
主要路径：
1. /mnt/data/csgo-datasets
   主数据源。顶层是 match/map hash，下面是具体 game/episode 数据。
2. /mnt/data/csgo-datasets-fullsubset
   更适合先做 v0 smoke test。抽样目录类似：
   32f1644d4f42c29d/030a506e5e77459c8d5078a06eec61ee
   下面有 navmesh.json、static_props.json，episode 里有每个玩家的 .mp4、_depth.mkv、_seg.mkv、_motion.bin、玩家状态 .json、_player_visibility.json。
3. /mnt/data/csgo-datasets-oss
   OSS 挂载镜像，结构类似 csgo-datasets；训练优先用 CPFS 路径，所以暂时不要优先扫这个。

限制：
- 不要删除、移动、覆盖任何已有数据。
- 不要安装包。
- 不要修改训练代码。
- 只允许新增 tools/scan_csgo_metadata.py 和 outputs/ 下的统计结果。
- 优先只读检查。
- 第一版不要全量 ffprobe 视频，避免慢；只统计文件存在性和大小。
- 如果不确定某字段含义，填空或 UNKNOWN，不要猜。

请按以下步骤做：

第一步：只读检查数据目录结构。
先重点检查 /mnt/data/csgo-datasets-fullsubset：
- find maxdepth 3 的目录结构；
- 抽样 100 个文件路径；
- 统计文件后缀/模态分布；
- 验证是否存在 .mp4、_depth.mkv、_seg.mkv、_motion.bin、.json、_player_visibility.json、navmesh.json、static_props.json。
然后再粗略检查 /mnt/data/csgo-datasets，但不要长时间全量扫描。

第二步：寻找当前训练 list / manifest。
在当前项目目录搜索：
manifest/train/val/test/split/list/dataset/json/jsonl/csv/txt
请报告：
- 找到的候选 manifest 路径；
- 每个 manifest 有多少条；
- 每条 entry 的格式；
- entry 是否能对应到 fullsubset 或 csgo-datasets 中的 basename。

第三步：创建 tools/scan_csgo_metadata.py。
脚本要求：
- 参数 --roots，默认优先 /mnt/data/csgo-datasets-fullsubset；
- 输出 --out_csv，默认 outputs/csgo_metadata.csv；
- 输出 --out_json，默认 outputs/csgo_metadata_summary.json；
- 按 basename group 统计 clip；
- 支持识别：
  .mp4
  _depth.mkv
  _seg.mkv
  _motion.bin
  _player_visibility.json
  普通玩家状态 .json
  navmesh.json
  static_props.json
- 字段至少包括：
  clip_id, root, dir, basename,
  match_hash, map_hash, episode, team, player, inst,
  has_rgb, has_depth, has_seg, has_motion,
  has_player_state, has_player_visibility,
  has_navmesh, has_static_props,
  rgb_path, depth_path, seg_path, motion_path,
  player_state_path, player_visibility_path,
  navmesh_path, static_props_path,
  rgb_size_mb, depth_size_mb, seg_size_mb, motion_size_mb
- 从文件名中尽量解析 episode/team/player/inst；
- 从路径中尽量解析 match_hash/map_hash；
- 对异常文件跳过，不要中断；
- 输出 summary，包括：
  总 clip group 数；
  完整多模态 clip 数；
  各 root 的数量；
  缺失各 modality 的数量；
  episode 数；
  player 数；
  map/hash 数。

第四步：生成候选数据。
基于 metadata csv 筛选：
- has_rgb/depth/seg/motion/player_state/player_visibility 全为 True；
- 文件大小大于 0；
- 如果已找到训练 manifest，则排除已经在 train/val 中出现的 basename；
输出：
- outputs/candidate_complete_clips.csv
- 如果能排除训练已有数据，则输出 outputs/candidate_new_clips.csv。

最后输出简短报告：
1. 当前训练 manifest 在哪里；
2. 当前训练集多少 clip；
3. fullsubset 中有多少 basename group；
4. fullsubset 中完整多模态 clip 有多少；
5. 为什么原始数据很多但训练只用几百个；
6. 第一批候选新数据在哪里；
7. 哪些字段还需要问学长确认。

开始前先列出你准备执行的只读命令，不要直接改文件。
```

---

你现在这张文档图的最大作用是：**把你的脚本统计目标从“通用视频 metadata”修正成“CS:GO 多模态 clip group metadata”**。  
所以一定要把 `_seg.mkv` 和 `_player_visibility.json` 加进统计，不然你会漏掉最关键的 dense condition 相关字段。